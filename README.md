# A01_5512

## Overview
This project loads the California Housing dataset and generates a boxplot of median income (MedInc) to visualize its distribution and outliers.

## How to Run
1. Install dependencies: pip install -r requirements.txt
2. Run the script: python3 src/boxplot.py
3. Output is saved to figs/boxplot.png
